keep all ids at the same spot: Centralize ID management. Define a single source of truth for ID  │
│   generation and storage. Implement a consistent ID schema. Ensure all ID references resolve to    │
│   this central location. Use UUIDs. Implement ID registry. Centralize ID lookup. Standardize ID    │
│   format. Version ID schema. Audit ID usage. Ensure ID uniqueness. Document ID strategy.    